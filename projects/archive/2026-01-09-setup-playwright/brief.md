We need to setup playwright for this project. The configuration has to follow best practices for an expo project and we also need to document best practices for adding selectors such as test ids and arias during implementations for potential use as a claude code skill.

The first playwright test should be one that confirms that "Hello, World screen" created by this project @projects/archive/2026-01-09-expo-router-gluestack-setup 

Part of your implementation is to prove it's working by including a single specific command that another developer could run to verify this during a code review.

Before considering this done, you also need to verify it by running said command.